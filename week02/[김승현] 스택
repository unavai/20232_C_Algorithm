| 작성자  |   날짜   | 언어    |
| ------- | --------- | ------- |
| 김승현    | 23.09.26  | C  |

# Problem_Title

 - https://www.acmicpc.net/problem/10828
  

### 풀이 과정  



### 소스 코드

```LanguageC

#include <stdio.h>
#include <string.h>

int stack[10000];
int top = -1;

int empty() {
    if(top < 0) return 1;
    return 0;
}

void push() {
    int x;
    scanf("%d",&x);
    stack[++top] = x;
}

int pop() {
    if(empty()) return -1;
    return stack[top--];
}

int size() {
    if(empty()) return 0;
    return top + 1;
}

int top_f() {
    if(empty()) return -1;
    return stack[top];
}


int main()
{
    int n;
    char input[6];
    int value;
    scanf("%d",&n);
    
    for(int i=0; i<n; i++) {
    scanf("%s",input);
    if(strcmp(input,"push") == 0) push();
    else if(strcmp(input,"pop") == 0) printf("%d\n",pop());
    else if(strcmp(input,"size") == 0) printf("%d\n",size());
    else if(strcmp(input,"empty") == 0) printf("%d\n",empty());
    else if(strcmp(input,"top") == 0) printf("%d\n",top_f());
    }
    return 0;
}




```

### 결과 화면
